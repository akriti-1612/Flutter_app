import 'package:flutter/material.dart';

void main() {
  runApp(flood());
}

class flood extends StatelessWidget {
  List<String> news_content;

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      home: Scaffold(
          appBar: AppBar(title: Text('News'), backgroundColor: Colors.cyan),
          body:
      )
    );
  }
}
